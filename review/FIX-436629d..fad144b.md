# agent-demo 评审修复记录

**对应评审**：[REVIEW-436629d..fad144b.md](REVIEW-436629d..fad144b.md)（条目编号沿用该报告 §1）
**修复日期**：2026-09-11
**修复范围**：该报告 §6 修复优先级建议的 **P1-1～P1-3**（对应 M1 / M2 / M3 / M5 / M6 / L7）；M4 及 P2/P3 项不在本轮（见「遗留与未验证面」）
**验证结果**：`pytest` **616 通过 / 32 跳过 / 0 失败**（修复前 610/32，新增 M1 回归 6 例）；`ruff check agentcore plugins tests bot.py scripts` **全绿**（修复前未跟踪 ingest 脚本有 1×F401）。评审 M1 复现样例按 REVIEW-WORKFLOW §6 验收由「可复现」变「不可复现」：`_build_user_text(群聊事件('小助手 帮我查一下天气'))` 修复前返回 `'小助手 帮我查一下天气'`（唤醒词泄漏进 prompt），修复后返回 `'帮我查一下天气'`。

---

## P1-1（评审 M1）唤醒词命中后不剥前缀

| 项 | 内容 |
|---|---|
| 修复方式 | 新增共享模块 `plugins/qq_agent_adapter/wakewords.py`（契约：`load_wake_words` / `match_wake_word` / `strip_wake_word`，最长匹配、大小写不敏感），触发判定与剥前缀共用同一实现；`matcher._match_wake_words` 改为委托 `match_wake_word`（触发语义不变）；`pipeline._build_user_text` 经新增的 `_strip_trigger_prefix` 在剥旧 `PREFIX` 正则**之前**先剥唤醒词——仅群聊（`group_id` 非空）剥离，私聊不剥（私聊无前缀语义，开头的唤醒词可能是正文本身） |
| 改动文件 | `plugins/qq_agent_adapter/wakewords.py`（新增）、`plugins/qq_agent_adapter/matcher.py`、`plugins/qq_agent_adapter/pipeline.py` |
| 回归测试 | `tests/test_pipeline.py::TestWakeWordStripping`（6 例：基本剥离 / 最长匹配 / 大小写不敏感 / 唤醒词+旧前缀叠加 / 私聊保留 / 无唤醒词时旧前缀仍剥） |

## P1-2（评审 M5 / M6 / L7）ingest 脚本与样例数据治理

| 条目 | 修复方式 | 验证 |
|---|---|---|
| M5 docstring 声称「超限自动截断」失真 | docstring 改为如实描述：单文件 2MB **超限跳过**（`ingest_file` 抛 ValueError）、单来源最多 200 块（超出部分丢弃） | 代码阅读对照 `agentcore/rag/ingest.py:73-74,13,40` |
| M5 失败静默（退出码恒 0） | 逐文件失败计数，任一失败汇总打印并 `sys.exit(1)` | `py_compile` 通过；真实导入路径依赖 PG+embedding，属「未验证面」（见文末） |
| L7 `import glob` 死代码（提交即 CI 红） | 删除该 import | `ruff check … scripts` 全绿（修复前 F401 实证） |
| M6 97.8MB 样例未 gitignore | `.gitignore` 增加 `data/kb_samples/*` + `!data/kb_samples/hello.md` | `git check-ignore` 实测 9 个大文件已忽略；已跟踪的 `hello.md` 不受影响 |

## P1-3（评审 M2 / M3）文档与实现闭环

| 条目 | 修复方式 | 验证 |
|---|---|---|
| M2 README 触发规则排他表述、旧前缀用法失踪 | 「消息路由规则」改为：唤醒词 / 旧前缀（`ai ` `!ai ` `/ai `，由 `AGENT_PREFIX` 定义）/ @机器人 三者**取或**；补回旧前缀用法说明；写明开头匹配、不区分大小写、命中后唤醒词被剥掉；「消息防抖·群聊限制」一句同步 | grep 确认 `!ai` 回到 README；逐条对照 `_match_wake_words` 实现 |
| M3 outbound 文档缺口（上轮报告 M1/M2/M3 的真正闭环） | README/.env.example 补 `AGENT_OUTBOUND_MAX_TARGETS`（默认 4096；按插入顺序淘汰未持锁旧会话桶——措辞与 `_evict_idle` 实现一致，不称 LRU）；`MAX_WAIT` 注明**只约束窗口条数分量**、最小间隔是硬约束；节流覆盖范围补「附发文件」 | grep 两文件均含 `OUTBOUND_MAX_TARGETS`；README 覆盖范围含「附发文件」 |
| M3 附带：commit message 声称失真 | 已发布的 `fad144b` message 无法追溯修改，在此声明：其「outbound docs」「fix ruff UP038」两条声称与实现不符，以本 FIX 与评审报告为准 | — |

---

## 遗留与未验证面（如实记录）

1. **M4（P2）掩蔽性测试**：`tests/test_matcher.py` 两处 `monkeypatch.setenv("AGENT_PREFIX")` 仍不生效，本轮未动。
2. **L1/L2/L3（P2/P3）**：唤醒词无词边界、全角逗号不归一化、`AGENT_WAKE_WORDS` 动态读取 vs `PREFIX` 导入时冻结，均未改。
3. **上轮遗留重申**：`split_message` chunk 边界 `.strip()`（上轮 H1/P0）与 `_reconstruct_content_from_memory` 死代码（上轮 H2/P1）仍未修复。
4. **未验证面（REVIEW-WORKFLOW §8.3 声明）**：ingest 脚本与 `/kb samples` 的真实导入路径（PG + 远程 embedding）本地未端到端实跑；200 块截断告警与 `embed_many` 分批属 P2 未做。
   **2026-09-11 生产复核**：该未验证面已验证并坐实——`绝区零.md`（706KB，切 200 块）单请求提交被 DashScope 兼容模式以 400 拒绝（线3 M3 推演成真）。已在 `agentcore/embedding/client.py` 修复：`_remote_embed` 按 `EMBEDDING_BATCH`（默认 10，`.env.example` 已文档化）分批发送，失败时透出上游响应体与批次范围；回归见 `tests/test_embedding.py::TestRemoteEmbedding`（4 例）。
5. **工作区状态**：本轮全部改动未提交；工作区另有本次修复前即存在的未提交变更 `admin.py`、`test_rag.py`（评审 L6，非 P1 范围），提交时注意分开。
