# FIX 记录：REVIEW-46c85d1..6ec3f7c

对应评审：[REVIEW-46c85d1..6ec3f7c.md](REVIEW-46c85d1..6ec3f7c.md)（2H / 6M / 8L）。

## 验证口径

- 全量套件：**1055 passed / 43 skipped**（新增 21 条回归；变异复核 8 个修复点全部"改坏必失败"）
- `ruff check` + `ruff format --check` 全绿（113 files）
- 变异复核方式：逐点把实现改回缺陷版本跑对应测试文件，断言失败数 > 0 后还原
  （注意：变异脚本用文件内容还原而非 move，避免 pyc/mtime 校验残留把变异版
  .pyc 误判有效——本轮踩过：move 还原后同秒内 size 未变的场景曾让变异代码
  被误跑一次，清 `__pycache__` 后 20 连跑稳定通过）

## 逐项修复

### H-2 docker-compose 凭据（阻断项）
- `docker-compose.yml`：`POSTGRES_USER`/`POSTGRES_PASSWORD` 改为
  `${POSTGRES_USER:-qqagent}` / `${POSTGRES_PASSWORD:-qqagent}` 环境注入，
  默认值与 README/CONTRIBUTING/AGENTS 文档一致；注释声明个人密码不入 VCS、
  `@` 需 URL 编码
- `.env.example`：新增 `POSTGRES_USER`/`POSTGRES_PASSWORD` 占位说明
- README 数据库章节：补充凭据注入方式与 `@` → `%40` 编码说明
- **遗留（不可逆处置）**：`Yll@2468` 已推送进 git 历史（de97227），无法改写
  而不 force-push；**用户必须轮换该密码**。四处文档凭据值已一致

### H-1 表格渲染阻塞事件循环
- `plugins/qq_agent_adapter/outbound.py`：`render_table_png(tbl)` →
  `await asyncio.to_thread(render_table_png, tbl)`（渲染卸载到线程池，
  异常经 await 原样传播，语义不变）
- 回归：`TestTableToImageHardening::test_render_does_not_block_event_loop`
  （15×40 表格渲染期间 10ms 周期心跳最大间隔 <200ms）；变异复核：改回同步
  调用 → 心跳测试失败 ✓

### M-1 渲染零规模防护 + 未处理异常
- `agentcore/render/table.py`：
  - 规模护栏：列数 > 20 整表返回 None（降级纯文本）；行数 > 100 截断并注明；
    单元格 > 200 字符截断加省略号（入口预截断杜绝 O(n²) 输入放大）
  - 截断算法改二分（`_fit_text`，log n 次测量）
  - 字号自适应从七轮全量重算改两轮（基础字号测量 → 按比例缩一次重测）
- `outbound.py`：逐表 try/except（渲染异常等同 None 降级、发送异常记日志继续）；
  每回复表数上限 5（`DEFAULT_MAX_TABLES_PER_REPLY`），超出降级纯文本
- 回归：列数超限 None / 行数截断注明 / 超长单元格 / `_fit_text` 二分 /
  渲染异常降级 / 发送异常继续；变异复核：删列护栏 → 列数用例失败 ✓；
  删异常隔离 → 降级用例失败 ✓

### M-2 embedding 退避失效（81a8521 目标的真正落地）
- `agentcore/embedding/client.py`：`_enter_degraded` 改**无条件刷新**
  `_degraded_since`（首次进入才打 ERROR 日志）——探测失败也刷新窗口，
  降级态不再每次先吃满远程超时
- `tests/test_embedding.py`：`test_degraded_skips_remote_until_retry_interval`
  补第 4 步断言（探测失败后再调用仍跳过）
- 变异复核：守卫回退 → 第 4 步断言失败 ✓

### M-3 PG init 回退池泄漏
- `plugins/qq_agent_adapter/__init__.py`：回退逻辑抽为可测的
  `_init_memory(db_url, dim)`；except 分支先 `await memory.aclose()` 再回退
  （`PgMemoryStore.aclose` 对 pool=None 幂等）；aclose 自身失败不挡回退
- 回归：`TestInitMemoryFallback` 4 条（池被关闭 / 成功返回 PG / 空 URL 内存 /
  aclose 失败仍回退）；变异复核：删 aclose → 池关闭用例失败 ✓

### M-4 search 工具结果不过围栏（AGENTS.md §4 不变量）
- `agentcore/loop/engine.py`：tool 结果注入 messages 前，`search_web`/
  `search_multi` 过 `fence_untrusted`（`_UNTRUSTED_TOOL_RESULTS` 集合）；
  fetch_url 类结果自带围栏不重复包；其余工具（bot 自身产物）不围栏
- 回归：`TestSearchResultFence` 3 条（search_web 围栏 / search_multi 围栏 /
  普通工具不围栏）；变异复核：删围栏分支 → 2 条失败 ✓

### M-5 de97227 声称与 diff 不符
- commit 历史不可改（§3 声称管理）：H-2 修复后在 FIX 本记录声明；
  教训固化：今后提交前 `git diff --stat` 核对声称

### M-6 LLM 数值 env 空值 import 即崩
- `agentcore/llm/client.py`：`LLM_TEMPERATURE`/`LLM_MAX_TOKENS` 改
  try/except 兜底（空值/脏值回落 0.7/1024，与 embedding/pipeline 同模式）
- 回归：`tests/test_llm.py` 4 条（空温度 / 空 max_tokens / 脏值 / 合法值照常）；
  变异复核：删兜底 → 2 条失败 ✓

### L-2 admin 命令无 self 过滤
- `plugins/qq_agent_adapter/admin.py`：新增 `_not_self_message`（与
  matcher._is_self_message 同判据，独立实现避免反向 import）；10 个
  on_command 加 `rule=_not_self_message`；`_confirm_delete_rule` 开头先行排除
- 无独立回归用例（rule 级行为，走查确认注册完整；导入测试
  `test_admin_import.py` 23 条覆盖注册完整性）

### L-3 字体路径仅 editable 成立
- `agentcore/render/table.py`：字体加载改候选链（仓库字体 →
  `/usr/share/fonts/truetype/wqy/` → `/usr/share/fonts/wqy-zenhei/` →
  `/usr/local/share/fonts/`），与 `help_render.py` 的 `_FONT_CANDIDATES`
  同思路；wheel 安装（data/fonts 不在包内）时由系统字体兜底
- 回归：候选链加载逻辑由 `test_font_missing_returns_none`（显式路径不回落）
  与探测缓存用例守卫

### L-4 字体缺失降级不可见
- `agentcore/render/table.py`：`ensure_font_probed()`——进程内首次渲染前
  探测字体链，不可用打**一条**汇总 WARNING（替代每条含表回复刷屏），结果缓存
- 回归：`test_ensure_font_probed_caches`

### L-5 datetime.now() 无时区
- `agentcore/loop/engine.py`：日期锚点改 `datetime.now(ZoneInfo("Asia/Shanghai"))`
  （UTC 服务器对中文用户日期差一天的根因）；无 tzdata 回落系统本地时钟

### L-6 引用解析失败时群流真空
- `plugins/qq_agent_adapter/pipeline.py`：群流门控从「有 reply/forward 段即
  不附」改为「**已解析出内容**才不附」（`quote_resolved`/`fwd_resolved`）——
  解析失败回退附群流兜底，消除上下文真空
- 回归：`TestGroupContextAttachment` 5 条（引用解析成功不附 / 解析失败回退附 /
  转发解析失败回退附 / 转发解析成功不附 / 无引用照常附）；变异复核：门控回退
  → 2 条回退用例失败 ✓

### L-7 _env_bool 脏值 fail-open
- `plugins/qq_agent_adapter/outbound.py`：脏值（非 0/1/true/false 等）回落
  默认**并打 WARNING**（旧实现静默按默认走）

## 遗留/降级项

- **H-2 密码轮换**：`Yll@2468` 在 git 历史中，必须轮换（用户操作）
- **L-8 测试覆盖**：admin self 过滤的 rule 行为无独立用例（rule 级难构造，
  导入测试覆盖注册完整性）；字体候选链的系统路径分支无法在本机触发（仓库
  字体优先命中）；非 editable 安装的 wheel 打包（pyproject 未含 data/fonts）
  未做——候选链已缓解，彻底方案是把 data/fonts 纳入 wheel（下轮评审可议）
