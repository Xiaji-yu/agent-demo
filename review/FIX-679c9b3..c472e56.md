# 修复记录：REVIEW-679c9b3..c472e56

**对应评审**：[`review/REVIEW-679c9b3..c472e56.md`](REVIEW-679c9b3..c472e56.md)（3 线并行子代理 + 主代理实证）
**修复方式**：逐条读码复核 → 修复 → 回归用例 → 「故意改坏实现 → 用例必须失败」变异复核（下表给出变异点）
**记法**：沿用上轮约定，问题编号锚定评审报告（M1–M4、L1–L11）

---

## 1. M 级（4 条全修）

| 编号 | 改法 | 回归 |
|---|---|---|
| **M1** debounce `max_parts=1` 会话永久无回复 | `debounce.py` burst 分支加 `if entry["task"] is not None` 守卫（原实现对新建 entry 的 `task=None` 调 `cancel()` 抛 AttributeError，且抛在 `pop` 之前 → entry 残留）。主代理复现结论**独立复核成立**：`Debouncer(delay=0.5, max_parts=1)` 连推 2 条，两条都炸、`_pending` 残留 `['k']`、runner 执行 0 次 | `tests/test_review_5_fixes.py::TestDebounceMaxPartsOne`（2 条：单条送达 + 连发 5 条每条独立成批）。**变异**：删守卫 → 2 条全红 |
| **M2** `.env.example` 默认唤醒词含 `ai` | 默认值改 `AGENT_WAKE_WORDS=小助手,助手`（与 README 示例对齐）；`ai` 保留在注释作可选示例，并写明「无词界前缀匹配，`ai` 会命中 airpods 等」的后果 | 文档项；`wakewords` 匹配语义未动，既有用例全绿 |
| **M3** 上轮漏修的 2 条假通过用例 | ① `test_get_bot_prefers_self_id`：`assert callable` → 3 条真实验证（指定 self_id 精确选 bot / 指定 bot 缺线回落 + WARNING / 无 bot 返回 None），fake driver 注入 `nonebot.get_driver`；② `test_zero_delay_runs_immediately`：断言移到 `push` 返回**之后立即**（不再 `sleep(0.01)` 后计数）——实现若改成 create_task 异步派发必然红 | `tests/test_pipeline.py::TestBotRouting`（3 条）/ `tests/test_debounce.py::test_zero_delay_runs_immediately`。**变异**：get_bot 忽略 preferred → `test_get_bot_prefers_self_id` 红；`delay<=0` 改 `delay<0` → zero_delay 红 |
| **M4** §9 索引断链 | `REVIEW-WORKFLOW.md` §9 补 `REVIEW-a604023..679c9b3` 行；§9.1 补 `FIX-a604023..679c9b3` 与本 `FIX-679c9b3..c472e56` 两行（增量链入口恢复） | 文档项（上表即产物） |

## 2. L 级（11 条全部处置）

| 编号 | 改法 |
|---|---|
| **L1** 契约文档自相矛盾 | `store.py` ABC docstring 逐一列明「非正 → 空结果」的适用方法，**注明唯一例外 `get_history` 钳到 1**（契约测试锁定的行为） |
| **L2** 并发测试单例泄漏 | `test_review_concurrency_fixes.py` 两处裸赋值 `m._turn_semaphore = None` 改 `monkeypatch.setattr`（测试后恢复原值，不再把进程内单例固定成 `Semaphore(2)`） |
| **L3** 停机 flush 无界等待 | `lifecycle.py` 新增 `AGENT_SHUTDOWN_FLUSH_TIMEOUT`（默认 30s；`<=0` = 退回无限等待旧行为；脏值记 WARNING 按 30s）。flush 出去的窗口要过全局 LLM 闸门，4 个在途回复各挂 60s 读超时时无界排队会被 systemd SIGKILL——现在到点放弃剩余窗口（记 ERROR）并保证 `aclose` 干净执行。`.env.example` 已入档 | 
| **L4** 突发任务无引用 | `debounce.py` 新增 `_spawn()`：强引用集合 + done_callback 自动移除；两个 `create_task` 站点统一走它，burst 任务命名 `debounce-burst:<key>` |
| **L5** zip 等号误伤 | `=` 拒绝收窄到**以 `-` 开头的参数**（`--unzip-command=cmd` 本来就活不过白名单，原检查只剩误伤 `report_v=2.zip` 这类合法文件名操作数）。`TestZipWhitelist` 4 条拒绝用例原样通过（安全面无松动） |
| **L6** README 残留「前缀」措辞 | 「消息防抖」群聊限制一句重写：不再出现「前缀」，并把因果写对（半句不触发 → 防抖窗口根本不开） |
| **L7** 重复用例 | 删除 `test_group_no_match_when_wake_words_and_prefix_both_miss`（与 `test_group_no_match_is_rejected` 场景逐字节相同，且名字指向已删除的 AGENT_PREFIX、设置残留配置 env） |
| **L8** M 级计数三种口径 | 统一为 **19**（REVIEW §2 表格行数：注入 3 / 存储 4 / 备份归档蒸馏 6 / 并发 6）。「17」是批二时点数——存储第 4 条（负 limit）与备份第 6 条（`_backup_jsonl` 整表 fetch）在第四批关闭；REVIEW 表头、`FIX-a604023..679c9b3.md` 导语、`BACKLOG.md` §6.1 三处已按此口径改齐 |
| **L9** BACKLOG 已修项写成遗留口吻 | §6.1 尾段逐项标 ✅（acl 私聊拒绝、format 门禁、PG 进 CI、CONTRIBUTING 版本、`AGENT_SKILLS_DIR`），并注明漏修 2 条假通过用例已在本轮关闭 |
| **L10** 环境变量核对无产物 | 结论入档：`LLM_EMBEDDING_MODEL` 未入 `.env.example` 是因为 `LLMClient.embeddings` 是**无调用方的死代码路径**（BACKLOG §6.2 已有行），记录结论、不补假文档 |
| **L11** AGENTS.md 铁律与豁免冲突 | §1 分层铁律补「唯一既存豁免：`skills/file_sender.py` 受保护可选导入，不要按铁律修复；新代码仍不得新增平台依赖」 |

## 3. 变异复核汇总（全部亲手执行）

| 变异 | 结果 |
|---|---|
| M1 删 `task is not None` 守卫 | `TestDebounceMaxPartsOne` 2 条全红 ✅ |
| L3 删 flush deadline（恢复无限等待） | 超时用例红（且验证了 deadline 存在时 5s 内完成） ✅ |
| L4 burst 改回无引用 `create_task` | `TestBurstTaskKeepsReference` 红（第一版测试被 `_job` 任务掩蔽，已改为按任务名精确断言 burst 位点——教训：持引用断言必须定位到**具体站点**） ✅ |
| L5 恢复无差别拒 `=` | `test_operand_filename_with_equals_allowed` 红，安全拒绝用例不受影响 ✅ |
| M3a get_bot 忽略 preferred | 红 ✅ |
| M3b `delay<=0` 改 `delay<0` | zero_delay 红 ✅ |

## 4. 验证结果（实测）

```bash
.venv/bin/python -m pytest -q
# 885 passed, 52 skipped（937 collected）

TEST_DATABASE_URL="postgresql://qqagent:qqagent@127.0.0.1:5432/qqagent_test" .venv/bin/python -m pytest -q
# 928 passed, 9 skipped（9 = RUN_PERF 门控）

.venv/bin/python -m ruff check agentcore plugins tests bot.py scripts
# All checks passed!（本地 0.16.6；UP041：asyncio.TimeoutError → 内置 TimeoutError）
.venv/bin/python -m ruff format --check agentcore plugins tests bot.py scripts
# 114 files already formatted

RUN_PERF=1 .venv/bin/python -m pytest tests/test_perf.py -q   # 9 passed
.venv/bin/python scripts/perf_baseline.py                     # 无劣化
```

**CI 忠实模拟**（`/tmp/ci2` 全量 rsync 工作区，ruff 用 pin 的 **0.9.6**）：
check / format 全绿；默认 **884 passed / 53 skipped**、PG **927 passed / 9+1 skipped**。
与本地唯一的差异是 `tests/test_help_render.py`（`d303a5c` 引入，非本批文件）在模拟环境因
**无 CJK 字体**跳过——环境差异，非代码分歧；GitHub runner 的表现以 CI 实际运行为准。

## 5. 附带修复：HEAD 既有的 format 门禁违规（非本批引入）

实测发现 **HEAD（`231e2e2`）在 CI pin 的 `ruff==0.9.6` 下本就有 3 个文件 `format --check` 违规**
（`plugins/qq_agent_adapter/help_render.py`、`tests/test_help_render.py`、`tests/test_persistence.py`，
均为 `d303a5c` 前后引入的排版问题）——即这几次提交的 CI format 门禁应当是红的（`unknown`：本机 API
配额耗尽无法代查，请以 Actions 页面为准）。已按 0.16.6 重排，**0.9.6 与 0.16.6 对重排结果均接受**
（双版本各跑 `format --check` 全绿），否则本批任何提交都会继承门禁红。

教训（已补进 AGENTS.md §5）：本地 ruff 漂移不只是「判定不一致」，重排结果也会随版本变化；
提交前用 pin 版本跑一遍 `format --check` 再推。

## 6. 遗留与移交

- 报告「存疑」三项（`max_parts=1` 真实使用面、水位线乱序重建分叉、plan-job 被 `/kb file` 抢注同名）**未验证**，维持原状；
- 报告 §6 建议的 P3 之外事项（perf 套件接 CI、DeepSeek thinking fallback）仍按 BACKLOG/报告原状，未在本批展开；
- 本批全部改动未提交、未推送（推送待用户明确指示）。
